// const cors_proxy = require('cors-anywhere');

// // Configure CORS Anywhere options
// const options = {
//   originWhitelist: [],  // Allow all origins
// };

// // Start the CORS Anywhere proxy server
// cors_proxy.createServer(options)
//   .listen(4201, 'localhost', () => {
//     console.log('CORS Anywhere proxy server started on localhost:8080');
//   });